#include <Arduino.h>
#include "./command.h"
#include <avr/delay.h>
#include <EEPROM.h>

void Coreo::execute(Servo s[],uint8_t curr, proto[] pr){
        for (uint8_t i=0;i<this.angle;i++)
        {

        }
}
